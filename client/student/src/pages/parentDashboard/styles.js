import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    paddingBottom: "200px",
  },

  heading: {
    padding: "30px 0px 40px",
  },

  card: {
    maxWidth: "300px",
    height: "180px",
  },
});
