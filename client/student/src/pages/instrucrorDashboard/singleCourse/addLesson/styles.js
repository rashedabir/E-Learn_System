import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
  root: {
    background: "#fff5f6",
    padding: "50px 0",
  },
  containers: {
    backgroundColor: "white",
    borderRadius: "6px",
    padding: "30px",
  },
});
