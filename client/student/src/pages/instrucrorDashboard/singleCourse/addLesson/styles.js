import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
    root: {
        backgroundColor: '#eee',
        padding: "20px 30px",
    },
    containers: {
        backgroundColor: "white",
        borderRadius: "12px",
    },
});
