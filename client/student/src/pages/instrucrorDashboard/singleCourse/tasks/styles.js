import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
    root: {

    },
    heading: {
        display: 'flex',
        alignItems: "center",
    },
    headingWrapper: {
        width: "100%",
        display: "flex",
        justifyContent: "space-between",
    },
    footer: {
        display: "flex",
        justifyContent: "space-between"
    }
});
