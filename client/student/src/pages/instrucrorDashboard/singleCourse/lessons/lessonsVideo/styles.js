import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
    root: {

    },
    title: {
        color: 'black',
        fontWeight: 600,
        marginBottom: '8px',
        marginTop: '4px',
    },
    video: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        paddingBottom: '8px',
    }
});
