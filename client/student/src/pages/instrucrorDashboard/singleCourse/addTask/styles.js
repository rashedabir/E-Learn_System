import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
  root: {
    backgroundColor: "#eee",
    padding: "40px 0",
  },
  containers: {
    backgroundColor: "white",
    borderRadius: "12px",
    padding: "50px",
  },
});
