import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
    root: {
        maxWidth: "100%"
    },
    dashboard: {
        paddingBottom: "20px",
        textAlign: "center",
    }
})