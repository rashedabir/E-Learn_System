import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
    root: {
        maxWidth: "100%",
        color: '#645A53',
    },
    linktxt: {
        textDecoration: 'none',
    },
    dashboard: {
        paddingBottom: "20px",
        textAlign: "center",
    }
})