import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
  root: {
    maxWidth: "100%",
    backgroundColor: "#fff5f6",
    paddingTop: "20px",
    minHeight: "90vh",
  },
});
