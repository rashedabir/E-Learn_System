import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
  root: {
    maxWidth: "100%",
    backgroundColor: "#eee",
    paddingTop: "30px",
    minHeight: "90vh",
  },
  search: {
    backgroundColor: "white",
  },
});
