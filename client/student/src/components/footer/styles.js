import { makeStyles } from "@mui/styles";

export const useStyle = makeStyles({
    root: {
        flexGrow: 1,
    },
    footer: {
        marginTop: "20px",
        paddingBottom: "15px",
        backgroundColor: "#EA5252",
        textAlign: "center",
        color: "white",
    },
});
