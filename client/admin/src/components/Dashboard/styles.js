import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  heading: {
    padding: "30px 0px",
  },

  card: {
    padding: "35px 20px",
  },
  
});
export const icon={
    fontSize:"40px"
 }